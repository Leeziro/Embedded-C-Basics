#include <stdio.h>

int main(void)
{
    int speed = 10;
    float temp = 23.252543f;
    char value = 'K';

    printf("Speed is: %d\n", speed);
    printf("temp is: %.4f\n", temp);
    printf("%c is ready to be eaten \n", value);

    return 0;
}